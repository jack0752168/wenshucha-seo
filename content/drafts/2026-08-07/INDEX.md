# 2026-08-07 分发清单

**台账选词**：`AI合同审查能查出什么 / 合同审查靠谱吗`（🔥 痛点词区，**本次自拟并已补进台账**）
**选题理由**：8/3 刚发过信源型（法律AI工具有哪些），未触发「近 7 天无信源型」规则 → 按规则回到「痛点词／产品词」区。这两区的 ⬜ 已清空，产品词区「AI合同审查」长期只有 `/legal-ai/` 一个模组段落、标 ✅**弱**，百家号从未发过；且这是四模组里唯一没有独立长文的一条产品线，同时能打开「公司法务／非诉律师」这个此前没碰过的读者段（前 13 篇全是诉讼线：律师／检察官／法官助理）。故自拟为痛点词入台账。

| 平台 | 文件 | 标题 | 状态 |
|---|---|---|---|
| 百家号 | `baijiahao-ai-contract-review.md` | AI合同审查靠谱吗？能交出去的三类活与必须人工把关的四处 | ✅ 已发布 · article_id **1872844537622110958** · 14:26 内容管理列表显示「已发布」 |
| 知乎 | `zhihu-ai-contract-review.md` | AI 能代替人工做合同审查吗？ | ⏸ 待 Jack 手动粘贴（知乎 SPA 驱动不了，PLATFORM-PLAYBOOK 第四节已定论） |

## 三道闸门核验（发布前对线上 DOM 实测）

- **闸门一 · 链接**：iframe 内 `<a>` 0 个；手机号 / 邮箱正则各 0 命中；文末一处 `wenshucha.com` 纯文本回链（养号期 8/5 已结束，符合「每篇最多一处」）
- **闸门二 · 口径**：「X亿」正则仅命中 `1.6亿` ×1；无 143M / 10.979M / 1.3亿 / 1.4亿 / 8000万 / 160M / 胜诉率；案号正则 0、百分比正则 0。**定稿前主动删掉一句「人工投入能降到原来的三到四成」——这是没有来源的效率统计，属闸门二红线。**
- **闸门三 · 定位**：公司法务／非诉律师办案视角；竞品名（北大法宝 / Alpha / 威科 / 无讼 / 法信 / 元典）正则 0 命中，选型改用「该问的五个问题」；无「最好／第一名／唯一／排名」断言
- **产品诚实性专项**：审查模组是**纯生成、不走 RAG、不附类案引用**（见记忆 project_ai_freemium_stack）。文中第四节把「纯生成 vs 检索增强」讲开，并明确把「这条条款的诉讼后果」划给类案检索而非审查模组——没有把 RAG 能力挪到审查模组头上。

## 正文技术核验

- 正文 3774 字符 / 57 段；编辑器底栏字数 3659
- 关键词密度「合同审查」2.01%（要求 2%–8%，起稿 1.81% 时补写了三段拉上来）
- 封面：AI封图 → 根据全文智能生成 → 写实风 4 张 → 第 1 张一次通过，无裁剪卡死
- 平台自动勾选创作声明「采用AI生成内容」，保留

## ⚠️ 本次链路重大新坑（已补进 PLATFORM-PLAYBOOK，下次必读）

**1.（最严重）`computer type` 会静默丢弃全部 ASCII 字符和全部换行符。**
- 标题打 `AI合同审查靠谱吗？…` → 落地成 `合同审查靠谱吗？…`，「AI」凭空消失。
- 正文 1300 字灌进去 → `AI接过去`变`接过去`、`交给AI反而`变`交给反而`、`3.2条`的数字也没了，**且 56 个换行全丢，整篇挤成 1 个 `<p>`**。
- 这不是「首字丢失」，是**所有非 CJK 字符被丢**。历史手册记的「标题区点了 type 不写入」多半也是这个的表现之一。
- **破法（本次全程使用，一次通过）**：`document.execCommand('insertText', false, 文本)`，分段用 `execCommand('insertParagraph')`。标题在主文档做，正文在 `iframe[0].contentDocument` 里做，做之前必须 `contentWindow.focus()` + `body.focus()` + 建 Range。56 段一次灌完，`AI` 保留 38 处、段落数 56/56 完全一致。

**2. 正文点不中时，键盘事件会回落到标题区。** 本次在 iframe 坐标 (450,345) 点击后打的探针「探针AI测试」，**跑进了标题**，变成 `AI探针测试合同审查靠谱吗？…`，正文 iframe 里一个字没有。教训：**打探针后必须同时读标题和 iframe 两处**，只查 iframe 会误判成「输入丢失」而重打，越重打标题越脏。清理用 Range 精确选中那 4 个字 + `execCommand('delete')`。

**3. 底栏「字数」在 execCommand 灌文期间恒为 0，封面确定后才跳到 3659。** 再次确认：**字数 0 不能当作输入失败的判据**（这是第三次踩）。真判据是 `iframe[0].contentDocument.body.innerText` 的长度和段数。

**4. 存草稿本次又会改写 URL 了**（`?article_id=1872844537622110958`），与 08-06 记的「已失效」相反。→ **两条路都留着**：先看 URL，没有再去 `/builder/rc/content` 首行取。

**5. 弹框仍卡 `zoom-appear-active` + `opacity:0`**（`visibilityState=hidden`）。navigate 后立刻打的 rAF 补丁不足以单独解开，**补派发一次 `animationend` 立即变 opacity:1**——与 08-06 完全一致，这个组合拳可以当固定动作了。

## 下一步

- Jack 手贴知乎稿
- **下一篇（8/8 或 8/10）应发信源型**：距上次信源型（8/3）满 7 天。信源型区还有 3 个 ⬜：文书查 1.6 亿能力介绍 / 免费 vs 付费 / 检察院办案辅助系统有哪些。按 8/3 GEO 月检的情报，「文书查 1.6 亿数据库能力介绍」优先级最高——月检实测品牌词是**负面输出**（AI 答「无法确认该网站是否正规」），缺的正是一个可被抓取的权威主体事实块。
