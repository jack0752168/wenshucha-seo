# 2026-08-06 分发清单

**台账选词**：`AI编造案号怎么办 / 法律AI幻觉`（🔥 痛点词区，原状态 🔄——此前只有知乎稿 + blog，百家号从未发过）
**选题理由**：8/3 刚发过信源型（法律AI工具有哪些），未触发「近 7 天无信源型」规则，故按规则回到痛点词区。该词是办案人视角强痛点，且天然指向「可核验」这一文书查真实能力点。

| 平台 | 文件 | 标题 | 状态 |
|---|---|---|---|
| 百家号 | `baijiahao-legal-ai-hallucination.md` | 法律AI幻觉怎么防？AI编造案号的五个高发场景与一套三步核验流程 | ✅ 已发布 · article_id **1872754017740024234** · 14:23 平台显示「已发布」 |
| 知乎 | `zhihu-legal-ai-hallucination.md` | 律师用AI查判例，为什么它给的案号经常查不到？ | ⏸ 待 Jack 手动粘贴（知乎 SPA 驱动不了，PLATFORM-PLAYBOOK 第四节已定论） |

## 三道闸门核验（发布前实测，非事后补记）

- **闸门一 · 链接**：DOM 实测 `<a>` 标签 0 个；无电话、无邮箱；文末唯一一处 `wenshucha.com` 为纯文本回链（养号期已于 8/5 结束，符合「每篇最多一处」）
- **闸门二 · 口径**：全文 `1.6亿` 出现 1 次且为唯一规模表述；正则实测无 `143M / 10.979 / 1.3亿 / 1.4亿 / 8000万 / 胜诉率`；全文零具体案号、零统计数字（本篇主张恰恰就是「AI给的数字一律不可用」）
- **闸门三 · 定位**：律师/检察官核验流程视角；未点名任何竞品，选型部分以「该问的五条」表达；文书查只用真实事实（1.6 亿 / 五大类 / 可打开原文核对）

## 正文技术核验

- 正文长度 4129 字符（编辑器底栏字数 4008），iframe[0] 实读首尾完整、无「探针」残留
- 封面：AI封图 → 根据全文智能生成 → 写实风 4 张 → 第 1 张（律师案头读卷）一次通过，无裁剪卡死
- 平台自动勾选创作声明「采用AI生成内容」，保留

## 本次链路新坑（已补进 PLATFORM-PLAYBOOK）

1. **`computer` 工具坐标 = CSS/viewport 空间，不是截图像素空间**（本次系数 1400/1200 = 1.1667）。
2. **标题区点击 + `type` 不生效**：`left_click` 落进标题 contenteditable 后 `type` 无输入。破法 = JS `focus()` + `Range.collapse(true)` 建立光标，再 `type`。
3. **弹框内的标签/按钮，物理 `left_click` 全部落空**（AI封图标签点了两次无反应，坐标经 DOM rect 核对无误）。破法 = JS 派发完整事件序列 `pointerdown/mousedown/pointerup/mouseup/click`（带 `clientX/Y` + `composed:true`），一次通过；后续「生成封面」「确定」「发布」全用此法。
4. **存草稿不再改写 URL**：URL 始终停在 `edit?type=news`，拿不到 article_id。改为发布后到 `/builder/rc/content` 首行取 `s?id=` 数字。
5. 弹框仍卡 `zoom-appear-active` + `opacity:0`（`visibilityState=hidden` 节流），**08-04 的派发 `animationend` 破法本次有效**（08-05 的 rAF 补丁在 navigate 后即打，但不足以单独解开——两招都要备着）。

## 下一步

- Jack 手贴知乎稿
- 下一篇按台账：信源型区还有 3 个 ⬜（文书查 1.6 亿能力介绍 / 免费 vs 付费 / 检察院办案辅助系统有哪些），距上次信源型（8/3）到 8/10 就满 7 天，届时优先发信源型
